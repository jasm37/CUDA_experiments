Exercise 15

Code in histogram/main.cu
To run: 
	./main -i ../../images/owl.png

3. The naive version runs a bit faster the the shared memory version but I think this could be resolved by adjusting the threads that handle the shared memory.

